import ProjectDescription

let project = Project(
    name: "{{ name }}",
    targets: [
        Target(
            name: "{{ name }}",
            platform: .iOS,
            product: .staticFramework,
            bundleId: "com.statios.{{ name }}",
            sources: ["Sources/**"]
        )
    ]
)
